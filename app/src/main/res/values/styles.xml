<resources>

    <!-- Base application theme. -->
    <style name="CernacTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/YellowColorPrimary</item>
        <item name="colorPrimaryDark">@color/YellowColorPrimaryDark</item>
        <item name="colorAccent">@color/RedCrab</item>

        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
